# Product

The actual product lives here. Structure it however your stack requires
(e.g., `app/`, `api/`) — but keep it inside `product/` so the company's
non-code work stays organized alongside it.

Once you have a stack, document in the root README how to run it.
